let index = 0

export default {

    pen: index++,
    line: index++,
    rectangle: index++,
    select: index++,
    move: index++,
    eraser: index++,
    picker: index++,
    bucket: index++,
    magic_bucket: index++

}